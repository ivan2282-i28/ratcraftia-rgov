# RGOV Frontend

React + Vite frontend for the Ratcraftia government portal.

## Local development

```bash
npm install
npm run dev
```

By default the app calls `/api`, and the Vite dev server proxies that path to `http://localhost:8000`.
