{
    auto_https off
}

:80 {
    @backend path /api/* /docs* /redoc* /openapi.json /docs/oauth2-redirect

    handle @backend {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:80
    }
}
